@model Web.ViewModels.Identity.PermissionViewModels
@{
    ViewBag.Title = "";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}



<div class="row">
    <div class="col-xs-4">
        <div class="row">
            @foreach(var item in Model.HtmlGroup.Where(c => c.Group == "Home"))
            {
              <div class="col-xs-12">
                  @Html.Raw(item.Html)
              </div>


            }
        </div>
        </div>
    <div class="col-xs-4">
        <div class="row">
            @foreach (var item in Model.HtmlGroup.Where(c => c.Group == "Home"))
            {
            <div class="col-xs-12">
                @Html.Raw(item.Html)
            </div>


            }
        </div>
    </div>
    <div class="col-xs-4">
        <div class="row">
            @foreach (var item in Model.HtmlGroup.Where(c => c.Group == "Home"))
            {
            <div class="col-xs-12">
                @Html.Raw(item.Html)
            </div>


            }
        </div>
    </div>
</div>
